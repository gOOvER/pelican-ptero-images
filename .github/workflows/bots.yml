name: build bots

on:
  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:
  schedule:
    - cron: "0 0 * * 1"
  push:
    branches:
      - main
    paths:
      - bots/**

permissions:
  contents: read
  packages: write

jobs:
  push:
    name: "bot:${{ matrix.tag }}"
    runs-on: ubuntu-latest
    timeout-minutes: 60
    concurrency:
      group: ${{ github.workflow }}-${{ github.ref }}-${{ matrix.tag }}
      cancel-in-progress: true
    strategy:
      fail-fast: false
      matrix:
        tag:
          - buzz
          - nodemongo
          - nodemongo7
          - nodemongo8
          - nodemongo83
          - parkertron
          - sogebot

    steps:
      - uses: actions/checkout@v7

      - uses: docker/setup-qemu-action@v4

      - uses: docker/setup-buildx-action@v4
        with:
          buildkitd-flags: --debug

      - name: Compute lowercase owner
        id: vars
        shell: bash
        run: |
          echo "owner=${GITHUB_REPOSITORY_OWNER,,}" >> "$GITHUB_OUTPUT"

      - uses: docker/login-action@v4.5.2
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - uses: docker/build-push-action@v7
        with:
          context: ./bots/${{ matrix.tag }}
          file: ./bots/${{ matrix.tag }}/Dockerfile
          platforms: linux/amd64
          push: true
          network: host
          tags: |
            ghcr.io/${{ steps.vars.outputs.owner }}/bots:${{ matrix.tag }}
            ghcr.io/${{ steps.vars.outputs.owner }}/bots:${{ matrix.tag }}-${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
        timeout-minutes: 45
